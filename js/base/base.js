"use strict";

// Loaded?
document.addEventListener("readystatechange", function () {
	switch (this.readyState.toLowerCase()) {
		case "loading": {
			break;
		};
		case "interactive": {
			break;
		};
		case "complete": {
			break;
		};
	};
});
